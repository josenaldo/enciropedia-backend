'use strict';

/**
 * evento-biografico service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::evento-biografico.evento-biografico');
